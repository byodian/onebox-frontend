import React from 'react';
import Logo from './Logo';
import { Container } from './FooterStyles';

const Footer = () => {
  return (
    <Container>
      <Logo />
    </Container>
  );
};

export default Footer;
